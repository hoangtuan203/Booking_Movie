server:
  port: 8080
  servlet:
    context-path: /user

spring :
  application:
    name: user-service
  datasource:
    url: "jdbc:mysql://localhost:3306/book-movie"
    driverClassName: "com.mysql.cj.jdbc.Driver"
    username: root
    password:
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    database-platform: org.hibernate.dialect.MySQLDialect
